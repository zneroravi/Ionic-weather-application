# Ionic 3 Weather Application

This is an application that uses the api(wunderground) to fetch weather data.


### Installation

Install the dependencies

```sh
$ npm install
```

### Serve
To serve in the browser

```sh
$ ionic serve
```
### Build Android APK File
To build APK file to output folder

```sh
$ ionic build android
```
